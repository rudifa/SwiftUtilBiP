platform :ios, '11.0'

target 'SwiftUtilBiPIOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftUtilBiP
  # none yet, but we need a workspace for travis

  target 'SwiftUtilBiPIOSTests' do
    inherit! :search_paths
    # Pods for testing
  end
    
end
